--- crates/gneiss-rtk/src/engine/ppp_fg.rs
+++ crates/gneiss-rtk/src/engine/ppp_fg.rs
@@ -107,6 +107,11 @@
             if res_pr.abs() > 100.0 {
                 tracing::warn!("HUGE res_pr: sat={:?}, meas={:.2}, exp={:.2}, res={:.2}, dist={:.2}, clk={:.2}, isb={:.2}", 
                     sat.sat, meas_pr, exp_pr, res_pr, dist, clk, sat.isb);
+                
+                // Drop measurements with huge pre-fit residuals to prevent state divergence
+                tracing::warn!("Dropping satellite {:?} due to huge pre-fit residual ({:.2}m)", sat.sat, res_pr);
+                continue;
             }
             let res_rr = meas_rr - exp_rr;
             

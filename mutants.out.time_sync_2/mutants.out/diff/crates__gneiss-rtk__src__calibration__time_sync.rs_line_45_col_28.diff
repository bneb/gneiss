--- crates/gneiss-rtk/src/calibration/time_sync.rs
+++ replace += with -= in cross_correlate_time_offset
@@ -37,17 +37,17 @@
                 let dt = t1 - t0;
                 if dt > 0.0 {
                     let alpha = (t_eval - t0) / dt;
                     let v_interp = v0 + (v1 - v0) * alpha;
                     
                     let x = gnss_vel[i];
                     let y = v_interp;
                     
-                    sum_xy += x.dot(&y);
+                    sum_xy -= /* ~ changed by cargo-mutants ~ */ x.dot(&y);
                     sum_xx += x.norm_squared();
                     sum_yy += y.norm_squared();
                     count += 1;
                 }
             }
         }
 
         if count > 10 && sum_xx > 1e-6 && sum_yy > 1e-6 { 

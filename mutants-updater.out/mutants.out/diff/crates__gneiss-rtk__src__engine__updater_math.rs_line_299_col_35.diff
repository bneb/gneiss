--- crates/gneiss-rtk/src/engine/updater_math.rs
+++ replace * with + in compute_loose_coupling_innovations
@@ -291,17 +291,17 @@
     state_vel: &nalgebra::Vector3<f64>,
     gnss_pos: &nalgebra::Vector3<f64>,
     gnss_vel: &nalgebra::Vector3<f64>,
     lever_arm: &nalgebra::Vector3<f64>,
     omega_b: &nalgebra::Vector3<f64>,
 ) -> DVector<f64> {
     let l_e = r_b_e * lever_arm;
     let pos_apc = state_pos + l_e;
-    let v_apc = state_vel + r_b_e * omega_b.cross(lever_arm);
+    let v_apc = state_vel + r_b_e + /* ~ changed by cargo-mutants ~ */ omega_b.cross(lever_arm);
 
     let mut z = DVector::zeros(6);
     z.rows_mut(0, 3).copy_from(&(gnss_pos - pos_apc));
     z.rows_mut(3, 3).copy_from(&(gnss_vel - v_apc));
     z
 }
 
 

sed -i '' 's/let comp_pr_dd = .*/let m_w_rov_sat = 1.0 \/ el_rov_sat.max(0.001).sin();\n            let m_w_rov_ref = 1.0 \/ el_rov_ref.max(0.001).sin();\n            let h_zwd = m_w_rov_sat - m_w_rov_ref;\n            let zwd_dd = h_zwd * state.zwd;\n            let comp_pr_dd = ( (pos_apc - sat_vec_rov).norm() - (pos_apc - ref_sat_vec_rov).norm() ) - ( (base_coord_vec - sat_vec_bas).norm() - (base_coord_vec - ref_sat_vec_bas).norm() ) + tropo_dd + zwd_dd;/' crates/gneiss-rtk/src/engine/measurement.rs
